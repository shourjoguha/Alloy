I will implement the time constraint enforcement logic by refactoring the `TimeEstimationService` and `SessionGeneratorService` to use centralized heuristics and robust adjustment algorithms.

### 1. Centralize Time Estimation Configuration
- **Modify `app/config/heuristics.py`**:
    - Add `TIME_CONSTRAINT_TOLERANCE_PERCENT = 5` constant.
    - Update `TimeEstimationConfig` TypedDict to include `tolerance_percent`.
    - Update `TIME_ESTIMATION` dictionary to include `"tolerance_percent": 5`.
- **Refactor `app/services/time_estimation.py`**:
    - Import `TIME_ESTIMATION` from `app/config/heuristics.py`.
    - Update `TimeEstimationService.__init__` to use `TIME_ESTIMATION` as the default configuration, replacing the hardcoded `_default_config` method to eliminate duplication.

### 2. Implement Time Constraint Logic in Session Generator
- **Modify `app/services/session_generator.py`**:
    - Import `TIME_ESTIMATION` (or access via `TimeEstimationService`).
    - **Refactor `_fill_to_target_duration`**:
        - Implement an iterative loop (max 3-5 passes) to adjust duration until it falls within the 5% tolerance.
        - Use `TIME_ESTIMATION["tolerance_percent"]` instead of hardcoded 0.95/1.05.
    - **Fix `_trim_over_duration`**:
        - Fix the bug where it attempts to reduce accessory sets but iterates over `main` exercises.
        - Implement a prioritized trimming strategy:
            1. Reduce cooldown (if > 5 min).
            2. Reduce accessory sets (if > 2).
            3. Remove accessory exercises (one by one).
            4. Reduce main sets (as a last resort).
    - **Improve `_fill_under_duration`**:
        - Append filler exercises to the `accessory` block instead of `main` to preserve workout structure.
        - Use appropriate default sets/reps for fillers.

### 3. Verification
- **Test the logic**:
    - Create a test script to simulate session generation with various durations and ensure the output respects the 5% buffer.
    - Verify that `TimeEstimationService` correctly picks up the configuration from `heuristics.py`.
